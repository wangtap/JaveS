一，Student   stu;
     这是在定义，在声明。
     stu是一个引用。
     Student s =new Student();
     stu=s;
     这是引用之间传替地址值 
     
     
     二。public void show (String a){}
     这里的a，也是一个引用。
     在调用show方法时，会传替一个参数到括号中这个参数就会被a接受
     
     三，public  共有的，表示在哪里都可以使用对象调用这个方法
     private
     私有的表示只能在本类中使用
     protected    受到保护的，同包下，或者子类都可以使用
     什么也不写，表示默认的，在同包下可以使用
     
     四什么是包，
     英文：package
     一个班级中，存在一个叫张三的同学，
     现在又来了一个叫张三的同学怎么办
     包的存在就是来区分同名的类
     包的命名 公司网址的倒置，这样能保证不同公司开发的类，即使同名了，也没有关系
     